# SimpleProxyChat Testing Servers

Yes... I used AI to generate the scripts to run them all at once. Bite me.

## Starting the Servers

On windows, simply run `start-all.bat`. It's pretty self-explanatory.
